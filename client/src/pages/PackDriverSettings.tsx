import CargoDriverSettings from "@/pages/CargoDriverSettings";

export default function PackDriverSettings() {
  // Reutiliza la misma configuración del conductor (vehículo, wallet, etc.).
  return <CargoDriverSettings />;
}

